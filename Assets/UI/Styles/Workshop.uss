/*
    Workshop, after lobby-prototype.html. This layout is the one wanted in
    game, so values here are the prototype's; change them only on purpose.

    Label rules that set margin or padding are written .lb-root .lb-x, so they
    outrank the label reset in Lobby.uss.

    Differences from the prototype, all USS limits:
      - empty slots have a translucent solid border; USS has no dashed border;
      - the gold ring round full slots is a child element, not box-shadow;
      - card and button shadows are sibling blocks, the picker's soft shadow
        a blurred one;
      - the grid is a wrapping row whose cards get their right margin from
        WorkshopPage (every third card has none), since USS has no gap and no
        :nth-child.
*/

:root {
    --workshop-slot-empty: rgba(244, 233, 213, 0.5);
    --workshop-slot-empty-edge: rgba(45, 35, 25, 0.45);
    --workshop-ready-ring: rgba(245, 195, 60, 0.7);
    --workshop-fam-round: rgb(127, 175, 99);
    --workshop-fam-square: rgb(126, 140, 168);
    --workshop-fam-triangle: rgb(195, 112, 90);
    --workshop-fam-combat: rgb(201, 139, 94);
    --workshop-veil: rgba(235, 220, 195, 0.55);
    --workshop-pick-lip: rgba(90, 70, 40, 0.4);
}

.lb-workshop-details {
    flex-shrink: 0;
    padding-left: var(--space-4);
    padding-right: var(--space-4);
    padding-top: var(--space-2);
}

.lb-workshop-details__text, .lb-era-message {
    white-space: normal;
    font-size: var(--type-caption);
    color: var(--ink-mute);
}

.lb-era-row {
    flex-direction: row;
    flex-wrap: wrap;
    margin-top: var(--space-1);
}

.lb-era-chip {
    min-width: var(--target-min);
    min-height: var(--target-min);
    margin-right: var(--space-1);
    margin-bottom: var(--space-1);
    padding-left: var(--space-2);
    padding-right: var(--space-2);
    border-radius: var(--corner-sm);
    background-color: var(--surface-raised);
    color: var(--ink);
    font-size: var(--type-caption);
    white-space: normal;
}

.lb-era-chip--locked {
    background-color: var(--surface-sunk);
    color: var(--ink-mute);
}

.lb-era-chip--equipped {
    background-color: var(--action);
    color: var(--action-ink);
}

.lb-workshop {
    flex-grow: 1;
}

/* ---- segmented switcher ---- */

.lb-wsbar {
    padding-top: var(--space-4);
    padding-left: var(--space-4);
    padding-right: var(--space-4);
}

.lb-wsseg {
    flex-direction: row;
    background-color: var(--surface-sunk);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
    border-top-left-radius: 29px;
    border-top-right-radius: 29px;
    border-bottom-left-radius: 29px;
    border-bottom-right-radius: 29px;
    padding-left: 4px;
    padding-right: 4px;
    padding-top: 4px;
    padding-bottom: 4px;
}

.lb-wsseg__btn {
    flex-grow: 1;
    flex-basis: 0;
    height: 44px;
    margin-right: 4px;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    border-top-left-radius: 22px;
    border-top-right-radius: 22px;
    border-bottom-left-radius: 22px;
    border-bottom-right-radius: 22px;
    transition-property: background-color;
    transition-duration: var(--motion-release);
}

.lb-wsseg__btn--last {
    margin-right: 0;
}

.lb-wsseg__btn > .lb-icon {
    width: 18px;
    height: 18px;
    margin-right: 7px;
    -unity-background-image-tint-color: var(--ink-mute);
}

.lb-root .lb-wsseg__text {
    font-size: 17px;
    color: var(--ink-mute);
}

/* The prototype's 0 2px 0 lift, as a bottom border. */
.lb-wsseg__btn--on {
    background-color: var(--surface-raised);
    border-bottom-width: var(--stroke-thin);
    border-bottom-color: var(--shadow-soft);
}

.lb-wsseg__btn--on > .lb-icon {
    -unity-background-image-tint-color: var(--ink);
}

.lb-root .lb-wsseg__btn--on > .lb-wsseg__text {
    color: var(--ink);
}

/* ---- slots ---- */

.lb-slots {
    flex-direction: row;
    justify-content: center;
    padding-top: var(--space-3);
    padding-bottom: var(--space-2);
    padding-left: var(--space-4);
    padding-right: var(--space-4);
}

.lb-slot {
    width: 72px;
    height: 72px;
    margin-right: var(--space-2);
    align-items: center;
    justify-content: center;
    background-color: var(--workshop-slot-empty);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--workshop-slot-empty-edge);
    border-right-color: var(--workshop-slot-empty-edge);
    border-top-color: var(--workshop-slot-empty-edge);
    border-bottom-color: var(--workshop-slot-empty-edge);
    border-top-left-radius: var(--corner-md);
    border-top-right-radius: var(--corner-md);
    border-bottom-left-radius: var(--corner-md);
    border-bottom-right-radius: var(--corner-md);
    white-space: normal;
    transition-property: scale;
    transition-duration: var(--motion-release);
    transition-timing-function: ease-out-back;
}

.lb-slot--last {
    margin-right: 0;
}

.lb-slot:active {
    scale: 0.94 0.94;
}

.lb-slot--filled {
    background-color: var(--surface-raised);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
}

.lb-root .lb-slot__text {
    font-size: 13px;
    color: var(--ink-mute);
    white-space: normal;
    -unity-text-align: middle-center;
}

.lb-root .lb-slot--filled .lb-slot__text {
    color: var(--ink);
}

.lb-root .lb-slot__letter {
    font-size: 24px;
    color: var(--ink);
    margin-bottom: 1px;
    -unity-text-align: middle-center;
}

/* The gold ring round a filled slot once every slot is full. */
.lb-slot__ring {
    position: absolute;
    left: -6px;
    top: -6px;
    right: -6px;
    bottom: -6px;
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--workshop-ready-ring);
    border-right-color: var(--workshop-ready-ring);
    border-top-color: var(--workshop-ready-ring);
    border-bottom-color: var(--workshop-ready-ring);
    border-top-left-radius: 19px;
    border-top-right-radius: 19px;
    border-bottom-left-radius: 19px;
    border-bottom-right-radius: 19px;
    display: none;
}

.lb-slotwrap--ready .lb-slot--filled > .lb-slot__ring {
    display: flex;
}

.lb-root .lb-readytag {
    height: 20px;
    font-size: 16px;
    color: var(--trophy-ink);
    -unity-text-align: middle-center;
    opacity: 0;
    transition-property: opacity;
    transition-duration: var(--motion-release);
}

.lb-root .lb-slotwrap--ready > .lb-readytag {
    opacity: 1;
}

/* ---- card grid ---- */

.lb-grid {
    flex-grow: 1;
    flex-shrink: 1;
    min-height: 0;
}

.lb-grid .unity-scroll-view__content-container {
    flex-direction: row;
    flex-wrap: wrap;
    align-content: flex-start;
    padding-top: var(--space-3);
    padding-left: var(--space-4);
    padding-right: var(--space-4);
    padding-bottom: 160px;
}

.lb-gcard-wrap {
    width: 31.33%;
    margin-bottom: var(--space-3);
    transition-property: opacity;
    transition-duration: var(--motion-release);
}

.lb-gcard-wrap--gap {
    margin-right: 3%;
}

.lb-gcard-wrap > .ui-sticker__shadow {
    left: 2px;
    top: 3px;
    right: -2px;
    bottom: -3px;
    background-color: var(--shadow-soft);
}

.lb-gcard-wrap--used {
    opacity: 0.38;
}

.lb-gcard-wrap--locked {
    opacity: 0.55;
}

.lb-gcard {
    align-items: center;
    padding-top: var(--space-2);
    padding-bottom: var(--space-2);
    padding-left: 3px;
    padding-right: 3px;
    background-color: var(--surface-raised);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
    border-top-left-radius: var(--corner-md);
    border-top-right-radius: var(--corner-md);
    border-bottom-left-radius: var(--corner-md);
    border-bottom-right-radius: var(--corner-md);
    transition-property: scale, translate;
    transition-duration: var(--motion-press);
    transition-timing-function: ease-in;
}

.lb-gcard:active {
    scale: 0.94 0.94;
    translate: 0 2px;
}

.lb-gcard__art {
    height: 48px;
    align-self: stretch;
    align-items: center;
    justify-content: center;
    margin-bottom: 4px;
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
    border-top-left-radius: var(--corner-sm);
    border-top-right-radius: var(--corner-sm);
    border-bottom-left-radius: var(--corner-sm);
    border-bottom-right-radius: var(--corner-sm);
}

.lb-art--round { background-color: var(--workshop-fam-round); }
.lb-art--square { background-color: var(--workshop-fam-square); }
.lb-art--triangle { background-color: var(--workshop-fam-triangle); }
.lb-art--combat { background-color: var(--workshop-fam-combat); }

/* TODO(art): a monogram stands where the item's art goes. */
.lb-root .lb-gcard__letter {
    font-size: 22px;
    color: var(--ink);
    -unity-text-align: middle-center;
}

.lb-gcard__art > .lb-icon {
    width: 14px;
    height: 14px;
    -unity-background-image-tint-color: var(--ink);
}

.lb-root .lb-gcard__name {
    font-size: 14px;
    color: var(--ink);
    -unity-text-align: middle-center;
    white-space: normal;
}

.lb-root .lb-gcard__note {
    font-size: 12px;
    color: var(--ink-mute);
    -unity-text-align: middle-center;
    white-space: normal;
    padding-left: 2px;
    padding-right: 2px;
}

/* ---- floating picker - draws OVER the cards ---- */

.lb-pickveil {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    background-color: var(--workshop-veil);
    opacity: 0;
    transition-property: opacity;
    transition-duration: var(--motion-release);
}

.lb-pickveil--on {
    opacity: 1;
}

.lb-picker {
    position: absolute;
    right: var(--space-3);
    bottom: 150px;
    flex-direction: column-reverse;
    align-items: flex-end;
}

.lb-pick-lip {
    top: 4px;
    bottom: -4px;
    background-color: var(--workshop-pick-lip);
}

.lb-pickbtn-wrap {
    position: relative;
}

.lb-pickbtn-wrap > .lb-pick-lip {
    border-top-left-radius: 31px;
    border-top-right-radius: 31px;
    border-bottom-left-radius: 31px;
    border-bottom-right-radius: 31px;
}

.lb-pickbtn {
    width: 62px;
    height: 62px;
    align-items: center;
    justify-content: center;
    background-color: var(--surface-raised);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
    border-top-left-radius: 31px;
    border-top-right-radius: 31px;
    border-bottom-left-radius: 31px;
    border-bottom-right-radius: 31px;
    transition-property: translate;
    transition-duration: var(--motion-press);
}

.lb-pickbtn:active {
    translate: 0 3px;
}

.lb-pickbtn > .lb-icon {
    width: 22px;
    height: 22px;
    -unity-background-image-tint-color: var(--ink);
}

.lb-root .lb-pickbtn__label {
    font-size: 10px;
    color: var(--ink-mute);
    letter-spacing: 0.4px;
    -unity-text-align: middle-center;
}

.lb-pickopts {
    align-items: flex-end;
    margin-bottom: var(--space-2);
    opacity: 0;
    translate: 0 14px;
    scale: 0.9 0.9;
    transform-origin: right bottom;
    transition-property: opacity, translate, scale;
    transition-duration: 160ms, 220ms, 220ms;
    transition-timing-function: ease-out, ease-out-back, ease-out-back;
}

.lb-picker--open > .lb-pickopts {
    opacity: 1;
    translate: 0 0;
    scale: 1 1;
}

.lb-pickopt-wrap {
    position: relative;
    margin-bottom: var(--space-2);
}

.lb-pickopt-wrap > .lb-pick-lip {
    border-top-left-radius: 28px;
    border-top-right-radius: 28px;
    border-bottom-left-radius: 28px;
    border-bottom-right-radius: 28px;
}

.lb-pickopt {
    height: 56px;
    flex-direction: row;
    align-items: center;
    padding-left: var(--space-4);
    padding-right: 20px;
    background-color: var(--surface-raised);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
    border-top-left-radius: 28px;
    border-top-right-radius: 28px;
    border-bottom-left-radius: 28px;
    border-bottom-right-radius: 28px;
    transition-property: translate;
    transition-duration: var(--motion-press);
}

.lb-pickopt:active {
    translate: 0 3px;
}

.lb-pickopt > .lb-icon {
    width: 18px;
    height: 18px;
    margin-right: 10px;
    -unity-background-image-tint-color: var(--ink);
}

.lb-root .lb-pickopt__text {
    font-size: 17px;
    color: var(--ink);
}

.lb-pickopt--on {
    background-color: var(--action);
    border-left-color: var(--action-edge);
    border-right-color: var(--action-edge);
    border-top-color: var(--action-edge);
    border-bottom-color: var(--action-edge);
}

.lb-root .lb-pickopt--on > .lb-pickopt__text {
    color: var(--action-ink);
}

.lb-pickopt--on > .lb-icon {
    -unity-background-image-tint-color: var(--action-ink);
}
