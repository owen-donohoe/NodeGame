/*
    Home - the lobby's landing page, after lobby-prototype.html.

    Tokens, shell and shared components (sticker shadows, lips, sheets) are
    in Lobby.uss, which LobbyRoot.uxml imports ahead of every page.

    Label rules that set margin or padding are written .lb-root .lb-x, so they
    outrank the label reset in Lobby.uss. Follow that in any new rule.
*/

:root {
    --home-chip: rgba(150, 128, 95, 0.2);
    --home-hat: rgb(201, 168, 95);
    --home-meter-bg: rgba(45, 35, 25, 0.8);
    --home-meter-track: rgb(74, 59, 42);
}

/* ===================================================================
   HOME
   =================================================================== */

.lb-home-page {
    flex-grow: 1;
    padding-left: 0;
    padding-right: 0;
    padding-top: 0;
    padding-bottom: 0;
}

/* ---- boxes ---- */

.lb-boxcol {
    position: absolute;
    left: var(--space-4);
    top: 24px;
    width: 130px;
}

.lb-box {
    align-items: center;
    justify-content: center;
    background-color: var(--surface-raised);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
    border-top-left-radius: var(--corner-md);
    border-top-right-radius: var(--corner-md);
    border-bottom-left-radius: var(--corner-md);
    border-bottom-right-radius: var(--corner-md);
    transition-property: scale, translate;
    transition-duration: var(--motion-press);
    transition-timing-function: ease-in;
}

.lb-box:active {
    scale: 0.96 0.96;
    translate: 0 2px;
}

.lb-box-wrap--big {
    height: 130px;
    margin-bottom: var(--space-3);
}

.lb-box-wrap--small {
    height: 78px;
}

.lb-box-wrap > .lb-box {
    flex-grow: 1;
}

.lb-box .lb-icon {
    width: 38px;
    height: 38px;
    -unity-background-image-tint-color: var(--ink-mute);
}

.lb-box-wrap--small .lb-icon {
    width: 26px;
    height: 26px;
}

.lb-root .lb-box__label {
    font-size: 14px;
    color: var(--ink-mute);
    margin-top: 4px;
    -unity-text-align: middle-center;
}

.lb-root .lb-badge {
    position: absolute;
    top: -11px;
    right: -11px;
    min-width: 30px;
    height: 30px;
    padding-left: 6px;
    padding-right: 6px;
    border-top-left-radius: 15px;
    border-top-right-radius: 15px;
    border-bottom-left-radius: 15px;
    border-bottom-right-radius: 15px;
    background-color: var(--badge);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--badge-edge);
    border-right-color: var(--badge-edge);
    border-top-color: var(--badge-edge);
    border-bottom-color: var(--badge-edge);
    color: var(--ink-white);
    font-size: 16px;
    -unity-text-align: middle-center;
}

.lb-root .lb-badge--zero {
    background-color: var(--off);
    border-left-color: var(--off-edge);
    border-right-color: var(--off-edge);
    border-top-color: var(--off-edge);
    border-bottom-color: var(--off-edge);
}

/* ---- loadout preview: deliberately quiet - no border, no shadow ---- */

.lb-loadcol {
    position: absolute;
    right: var(--space-4);
    top: 28px;
    align-items: center;
    opacity: 0.62;
    transition-property: opacity;
    transition-duration: var(--motion-release);
}

.lb-loadcol:hover {
    opacity: 1;
}

.lb-lpi {
    width: 40px;
    height: 40px;
    margin-bottom: 6px;
    border-top-left-radius: 11px;
    border-top-right-radius: 11px;
    border-bottom-left-radius: 11px;
    border-bottom-right-radius: 11px;
    background-color: var(--home-chip);
    align-items: center;
    justify-content: center;
}

.lb-lpi--gap {
    margin-top: 9px;
}

/* A filled slot: faded navy, with the item's letter. */
.lb-lpi--filled {
    background-color: var(--status-ready);
}

/* An empty slot the player could fill: faded orange. The same orange marks
   the short side in the battle sheet's loadout line. */
.lb-lpi--short {
    background-color: var(--status-short);
}

.lb-root .lb-lpi__letter {
    font-size: 19px;
    color: var(--ink-mute);
    -unity-text-align: middle-center;
}

.lb-root .lb-lpi--filled > .lb-lpi__letter {
    color: var(--status-ready-ink);
}

.lb-root .lb-lpcap {
    font-size: 11px;
    color: var(--ink-mute);
    letter-spacing: 0.7px;
    margin-top: 3px;
    -unity-text-align: middle-center;
}

/* ---- villager ---- */

.lb-villager-dock {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 30%;
    align-items: center;
}

.lb-root .lb-tapedit {
    font-size: 15px;
    color: var(--ink-mute);
    margin-bottom: 14px;
}

.lb-villager {
    align-items: center;
    transition-property: translate;
    transition-duration: var(--motion-bob);
    transition-timing-function: ease-in-out;
}

.lb-villager--up {
    translate: 0 -6px;
}

.lb-villager__body {
    width: 56px;
    height: 78px;
    background-color: var(--surface-raised);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
    border-top-left-radius: 26px;
    border-top-right-radius: 26px;
    border-bottom-left-radius: 12px;
    border-bottom-right-radius: 12px;
    align-items: center;
}

.lb-villager > .lb-sticker__shadow {
    top: 5px;
    bottom: -5px;
    border-top-left-radius: 26px;
    border-top-right-radius: 26px;
    border-bottom-left-radius: 12px;
    border-bottom-right-radius: 12px;
}

.lb-villager__hat {
    position: absolute;
    top: -11px;
    width: 52px;
    height: 14px;
    background-color: var(--home-hat);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--edge);
    border-right-color: var(--edge);
    border-top-color: var(--edge);
    border-bottom-color: var(--edge);
    border-top-left-radius: 9px;
    border-top-right-radius: 9px;
    border-bottom-left-radius: 9px;
    border-bottom-right-radius: 9px;
}

.lb-villager__face {
    position: absolute;
    top: 17px;
    width: 26px;
    height: 26px;
    -unity-background-image-tint-color: var(--edge);
}

/* ---- call to action ---- */

.lb-cta {
    position: absolute;
    left: var(--space-4);
    right: var(--space-4);
    bottom: 100px;
}

.lb-battle {
    height: 74px;
    background-color: var(--action);
    border-left-width: var(--stroke);
    border-right-width: var(--stroke);
    border-top-width: var(--stroke);
    border-bottom-width: var(--stroke);
    border-left-color: var(--action-edge);
    border-right-color: var(--action-edge);
    border-top-color: var(--action-edge);
    border-bottom-color: var(--action-edge);
    border-top-left-radius: var(--corner-lg);
    border-top-right-radius: var(--corner-lg);
    border-bottom-left-radius: var(--corner-lg);
    border-bottom-right-radius: var(--corner-lg);
    font-size: 30px;
    letter-spacing: 0.5px;
    color: var(--action-ink);
    transition-property: translate;
    transition-duration: var(--motion-press);
    transition-timing-function: ease-in;
}

.lb-battle:active {
    translate: 0 4px;
}

.lb-battle-lip {
    background-color: var(--action-edge);
    border-top-left-radius: var(--corner-lg);
    border-top-right-radius: var(--corner-lg);
    border-bottom-left-radius: var(--corner-lg);
    border-bottom-right-radius: var(--corner-lg);
}

.lb-boxmeter {
    margin-top: var(--space-3);
    flex-direction: row;
    align-items: center;
    background-color: var(--home-meter-bg);
    border-top-left-radius: 14px;
    border-top-right-radius: 14px;
    border-bottom-left-radius: 14px;
    border-bottom-right-radius: 14px;
    padding-top: 8px;
    padding-bottom: 8px;
    padding-left: 16px;
    padding-right: 16px;
}

.lb-root .lb-boxmeter__label {
    font-size: 15px;
    color: var(--ink-inverse);
    margin-right: var(--space-3);
    flex-shrink: 0;
}

.lb-boxmeter__track {
    flex-grow: 1;
    height: 12px;
    border-top-left-radius: 6px;
    border-top-right-radius: 6px;
    border-bottom-left-radius: 6px;
    border-bottom-right-radius: 6px;
    background-color: var(--home-meter-track);
    overflow: hidden;
}

.lb-boxmeter__fill {
    height: 100%;
    width: 0;
    background-color: var(--confirm);
}

