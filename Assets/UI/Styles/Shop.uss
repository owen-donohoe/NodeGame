/*
    Shop, after lobby-prototype.html.

    Label rules that set margin or padding are written .lb-root .lb-x, so they
    outrank the label reset in Lobby.uss.

    Differences from the prototype, all USS limits: the stall's drop-shadow is
    a sibling block under the awning and the counter rather than one shadow
    of the whole silhouette, and the list pills' 0 3px 0 lip is a bottom
    border.
*/

.lb-shop {
    flex-grow: 1;
}

/* Clears the floating tab bar at the bottom of the scroll. */
.lb-shop .unity-scroll-view__content-container {
    padding-bottom: 150px;
}

/* ---- the stall ---- */

.lb-stallzone {
    height: 322px;
    flex-direction: row;
    align-items: center;
    padding-left: var(--lb-sp4);
    padding-right: var(--lb-sp4);
}

.lb-stall {
    width: 180px;
    flex-shrink: 0;
    margin-right: var(--lb-sp3);
}

.lb-stall__awning-wrap > .lb-sticker__shadow {
    left: 4px;
    top: 7px;
    right: -4px;
    bottom: -7px;
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    border-bottom-left-radius: 0;
    border-bottom-right-radius: 0;
}

.lb-stall__awning {
    flex-direction: row;
    width: 180px;
    height: 36px;
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-width: var(--lb-bw);
    border-bottom-width: var(--lb-bw);
    border-left-color: var(--lb-edge);
    border-right-color: var(--lb-edge);
    border-top-color: var(--lb-edge);
    border-bottom-color: var(--lb-edge);
    border-top-left-radius: 8px;
    border-top-right-radius: 8px;
    overflow: hidden;
}

.lb-stripe {
    flex-grow: 1;
    flex-basis: 0;
}

.lb-stripe--red { background-color: var(--lb-awning-red); }
.lb-stripe--cream { background-color: var(--lb-awning-cream); }

.lb-stall__post {
    position: absolute;
    top: 36px;
    width: 10px;
    height: 56px;
    background-color: var(--lb-stall-wood);
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-width: var(--lb-bw);
    border-bottom-width: var(--lb-bw);
    border-left-color: var(--lb-edge);
    border-right-color: var(--lb-edge);
    border-top-color: var(--lb-edge);
    border-bottom-color: var(--lb-edge);
}

.lb-stall__post--left { left: 4px; }
.lb-stall__post--right { right: 4px; }

.lb-stall__keeper {
    position: absolute;
    left: 70px;
    top: 44px;
    width: 38px;
    height: 50px;
    background-color: var(--lb-paper-hi);
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-width: var(--lb-bw);
    border-bottom-width: var(--lb-bw);
    border-left-color: var(--lb-edge);
    border-right-color: var(--lb-edge);
    border-top-color: var(--lb-edge);
    border-bottom-color: var(--lb-edge);
    border-top-left-radius: 18px;
    border-top-right-radius: 18px;
    border-bottom-left-radius: 8px;
    border-bottom-right-radius: 8px;
}

.lb-stall__counter-wrap {
    margin-top: 56px;
}

.lb-stall__counter-wrap > .lb-sticker__shadow {
    left: 4px;
    top: 7px;
    right: -4px;
    bottom: -7px;
}

.lb-stall__counter {
    width: 180px;
    height: 52px;
    background-color: var(--lb-stall-counter);
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-width: var(--lb-bw);
    border-bottom-width: var(--lb-bw);
    border-left-color: var(--lb-edge);
    border-right-color: var(--lb-edge);
    border-top-color: var(--lb-edge);
    border-bottom-color: var(--lb-edge);
    border-top-left-radius: 6px;
    border-top-right-radius: 6px;
    border-bottom-left-radius: 10px;
    border-bottom-right-radius: 10px;
}

/* ---- wares ---- */

.lb-wares {
    flex-grow: 1;
}

.lb-ware-wrap {
    margin-bottom: var(--lb-sp3);
}

.lb-ware-wrap--last {
    margin-bottom: 0;
}

.lb-ware-wrap > .lb-sticker__shadow {
    background-color: rgba(90, 70, 40, 0.28);
}

.lb-ware {
    height: 74px;
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
    padding-left: var(--lb-sp3);
    padding-right: var(--lb-sp3);
    background-color: var(--lb-paper-hi);
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-width: var(--lb-bw);
    border-bottom-width: var(--lb-bw);
    border-left-color: var(--lb-edge);
    border-right-color: var(--lb-edge);
    border-top-color: var(--lb-edge);
    border-bottom-color: var(--lb-edge);
    border-top-left-radius: var(--lb-r-md);
    border-top-right-radius: var(--lb-r-md);
    border-bottom-left-radius: var(--lb-r-md);
    border-bottom-right-radius: var(--lb-r-md);
    transition-property: scale, translate;
    transition-duration: var(--lb-t-press);
    transition-timing-function: ease-in;
}

.lb-ware:active {
    scale: 0.96 0.96;
    translate: 0 2px;
}

.lb-ware > .lb-icon {
    width: 26px;
    height: 26px;
    margin-right: var(--lb-sp3);
    -unity-background-image-tint-color: var(--lb-ink);
}

.lb-root .lb-ware__name {
    font-size: 16px;
    color: var(--lb-ink);
    -unity-text-align: middle-left;
}

.lb-root .lb-ware__cost {
    font-size: 14px;
    color: var(--lb-ink-mute);
    -unity-text-align: middle-left;
}

/* ---- the quiet list ---- */

.lb-shop-tail {
    min-height: 520px;
    background-color: var(--lb-paper);
    border-top-width: var(--lb-bw);
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-color: var(--lb-edge);
    border-left-color: var(--lb-edge);
    border-right-color: var(--lb-edge);
    border-top-left-radius: var(--lb-r-sheet);
    border-top-right-radius: var(--lb-r-sheet);
    padding-top: var(--lb-sp5);
    padding-left: var(--lb-sp4);
    padding-right: var(--lb-sp4);
}

.lb-root .lb-sechead {
    font-size: 23px;
    color: var(--lb-ink);
    margin-bottom: var(--lb-sp3);
}

.lb-root .lb-sechead--spaced {
    margin-top: var(--lb-sp5);
}

.lb-row-wrap {
    margin-bottom: var(--lb-sp3);
}

.lb-row-wrap > .lb-sticker__shadow {
    background-color: var(--lb-shadow-soft);
}

.lb-row {
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
    padding-left: var(--lb-sp3);
    padding-right: var(--lb-sp3);
    padding-top: var(--lb-sp3);
    padding-bottom: var(--lb-sp3);
    background-color: var(--lb-paper-hi);
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-width: var(--lb-bw);
    border-bottom-width: var(--lb-bw);
    border-left-color: var(--lb-edge);
    border-right-color: var(--lb-edge);
    border-top-color: var(--lb-edge);
    border-bottom-color: var(--lb-edge);
    border-top-left-radius: var(--lb-r-md);
    border-top-right-radius: var(--lb-r-md);
    border-bottom-left-radius: var(--lb-r-md);
    border-bottom-right-radius: var(--lb-r-md);
}

.lb-rowthumb {
    width: 56px;
    height: 56px;
    flex-shrink: 0;
    margin-right: var(--lb-sp3);
    align-items: center;
    justify-content: center;
    background-color: var(--lb-card);
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-width: var(--lb-bw);
    border-bottom-width: var(--lb-bw);
    border-left-color: var(--lb-edge);
    border-right-color: var(--lb-edge);
    border-top-color: var(--lb-edge);
    border-bottom-color: var(--lb-edge);
    border-top-left-radius: var(--lb-r-sm);
    border-top-right-radius: var(--lb-r-sm);
    border-bottom-left-radius: var(--lb-r-sm);
    border-bottom-right-radius: var(--lb-r-sm);
}

.lb-rowthumb > .lb-icon {
    width: 24px;
    height: 24px;
    -unity-background-image-tint-color: var(--lb-ink);
}

.lb-rowthumb--large {
    width: 84px;
    height: 84px;
    margin-right: var(--lb-sp4);
}

.lb-rowthumb--large > .lb-icon {
    width: 34px;
    height: 34px;
}

.lb-row__text {
    flex-grow: 1;
    flex-shrink: 1;
    margin-right: var(--lb-sp3);
}

.lb-root .lb-row__title {
    font-size: 19px;
    color: var(--lb-ink);
    -unity-text-align: middle-left;
}

.lb-root .lb-row__sub {
    font-size: 15px;
    color: var(--lb-ink-mute);
    white-space: normal;
    -unity-text-align: middle-left;
}

.lb-pill {
    flex-shrink: 0;
    height: 44px;
    padding-left: var(--lb-sp4);
    padding-right: var(--lb-sp4);
    background-color: var(--lb-green);
    border-left-width: var(--lb-bw);
    border-right-width: var(--lb-bw);
    border-top-width: var(--lb-bw);
    border-bottom-width: 6px;
    border-left-color: var(--lb-green-dk);
    border-right-color: var(--lb-green-dk);
    border-top-color: var(--lb-green-dk);
    border-bottom-color: var(--lb-green-dk);
    border-top-left-radius: 22px;
    border-top-right-radius: 22px;
    border-bottom-left-radius: 22px;
    border-bottom-right-radius: 22px;
    font-size: 17px;
    color: var(--lb-green-text);
}

.lb-pill:active {
    translate: 0 2px;
}

/* ---- purchase sheet ---- */

.lb-shop-item__row {
    flex-direction: row;
    align-items: center;
    margin-bottom: 20px;
}

.lb-root .lb-shop-item__note {
    flex-shrink: 1;
    margin-bottom: 0;
}
